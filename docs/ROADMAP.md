# Roadmap

- Swap BasicTextField with a high-performance editor widget
- TextMate syntax highlighting + themes
- File tree + recent files
- Auto-save, crash recovery, large-file mode
- Extensible command palette (args, search)
- Plugin sandboxing (timeouts, capability prompts)
- Optional APK plugin bridge via AIDL
- LSP client (long-term)