# Texticle (OSS)

A fast, ad‑free text editor for Android built with Kotlin + Jetpack Compose. Open source, with a simple JavaScript plugin system.

## Build
- Android Studio Koala+ (AGP 8.6)
- Min SDK 26, Target SDK 35

```bash
./gradlew assembleDebug